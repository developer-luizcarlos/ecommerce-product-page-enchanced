const EmptyCart = () => {
  return (
    <article className="flex items-center justify-center h-full w-full">
      <p className="text-base text-DarkGrayishBlue font-semibold">Your cart is empty</p>
    </article>
  );
};

export default EmptyCart;
